import { ApplicationTableau } from 'src/app/modules/shared/model/Application';

describe('Application', () => {
  it('should create an instance', () => {
    expect(new ApplicationTableau()).toBeTruthy();
  });
});
